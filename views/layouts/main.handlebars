<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Registry</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/styles.css">
</head>

<body class="container">
  <header>
    <nav class="nav">
      <button class="btn btn-primary"><a class="nav-link active" aria-current="page" href="/">Home</a></button>
      <button class="btn btn-primary"><a class="nav-link active" aria-current="page" href="/dashboard">Dashboard</a></button>
    {{#if logged_in}}
     <button class="btn btn-primary" id="logout"><a class="nav-link active" aria-current="page" href="#">Logout</a></button>
    {{else}}
      <button class="btn btn-primary"><a class="nav-link active" aria-current="page" href="#">Login</a></button>
    {{/if}}
    </nav>
    <h1>The Tech Blog</h1>
  </header>
  <main class="container container-fluid mt-5">
    {{{ body }}}
    
  </main>
  <footer>
      
  </footer>

  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>